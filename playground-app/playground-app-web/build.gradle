apply plugin: 'war'

dependencies {
    compile project(':playground-app-ejb')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.web
        }
    }
}
